# Dashboard YAML
# Reference documentation: https://docs.rilldata.com/reference/project-files/dashboards

type: metrics_view

title: "Dashboard Title"
table: crm_history_gcs_model # Choose a table to underpin your dashboard
# timeseries:  # Select an actual timestamp column (if any) from your table

dimensions:
  - column: customer_id
    label: "Category"
    description: "Description of the dimension"

measures:
  - expression: "SUM(price_purchased)"
    label: "Total Revenue"
    description: "Total revenue generated"

  - expression: "SUM(revenue)"
    label: "Total Revenue"
    description: "Total revenue generated"
